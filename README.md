## DQN - Intelligent Ramp Metering Control with Partial Detection

### Build dependencies

make: `cd bin/ && bash make.sh`

### Note: to test other DRL Models you replace the content of the files (rl_controller and dqn_config and the model is save/ etc) with each correspoding files exsiting in the subfolders in custom_env/
